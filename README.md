# 鼠鼠打卡日記

每日打卡 AI 助理，粉色系手機版單頁應用。

## 本機開發
這是單一 HTML 檔案（`index.html`），不需要任何建置工具，直接用瀏覽器打開即可運作。
React / ReactDOM 已預先編譯內嵌在檔案中，不依賴外部 CDN。

## 部署
此專案接上 Netlify 後，每次 push 到 main 分支就會自動部署到同一個網址。

## 更新功能
之後請 Claude 修改 `index.html`，下載新版本後覆蓋這個檔案、提交（commit）即可自動部署。
